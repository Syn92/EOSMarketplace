export default class ServerConstants {
    static readonly ip = '10.0.0.7';
    static readonly local = `http://${ServerConstants.ip}:4000/`;
    static readonly prod = 'https://eos-marketplace.nn.r.appspot.com/';
};